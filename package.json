{
  "name": "your-project",
  "version": "1.0.0",
  "description": "A project with OpenAI chatbot and static files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^3.3.0",
    "express": "^4.17.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "mkdir -p build && cp -r src/* build/"
  }
}
