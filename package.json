{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "index.js",
  "scripts": {
    "start": "serve .",
    "deploy": "netlify deploy --prod --dir=."
  },
  "dependencies": {
    "serve": "^11.3.2",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "netlify-cli": "^11.5.0"
  }
}
